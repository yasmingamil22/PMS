/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SlicePipe } from './Slice.pipe';

describe('Pipe: Slicee', () => {
  it('create an instance', () => {
    let pipe = new SlicePipe();
    expect(pipe).toBeTruthy();
  });
});
